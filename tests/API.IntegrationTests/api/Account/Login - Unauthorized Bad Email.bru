meta {
  name: Login - Unauthorized Bad Email
  type: http
  seq: 1
}

post {
  url: https://localhost:5001/api/account/login
  body: json
  auth: inherit
}

body:json {
  {
      "email" : "testa@test.com",
      "password" : ""
  }
}

tests {
  test("Should return unathorized", function() {
    expect(res.getStatus()).to.equal(401);
  });
  
}

settings {
  encodeUrl: true
  timeout: 0
}
